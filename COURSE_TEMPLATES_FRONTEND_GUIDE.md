# 📚 Руководство для фронтенд-разработчика: Система шаблонов курсов

## 🎯 Концепция

Система позволяет создавать **родительские курсы (шаблоны)** с готовым контентом, который затем автоматически копируется во все **дочерние SubjectGroup'ы** (конкретные классы).

### Основные понятия:

- **Course (Курс)** — базовый курс, например "Математика 1 класс"
- **Template CourseSection (Шаблонная секция)** — секция курса с контентом, привязана к `Course`, а не к конкретному классу
- **SubjectGroup (Предметная группа)** — конкретная реализация курса для класса + учителя, например "Математика 1 класс для 1Б"
- **Derived CourseSection (Дочерняя секция)** — секция в SubjectGroup, скопированная из шаблона
- **Template Resource/Assignment** — ресурс/задание в шаблонной секции
- **Derived Resource/Assignment** — копия ресурса/задания в дочерней секции

---

## 📋 Пошаговая инструкция

### Шаг 1: Создание родительского курса

Создай базовый курс, который будет шаблоном:

```http
POST /api/courses/
Authorization: Bearer <superadmin_token>
Content-Type: application/json

{
  "course_code": "MATH1",
  "name": "Математика 1 класс",
  "description": "Базовый курс математики для первого класса",
  "grade": 1
}
```

**Ответ:**
```json
{
  "id": 1,
  "course_code": "MATH1",
  "name": "Математика 1 класс",
  "description": "Базовый курс математики для первого класса",
  "grade": 1
}
```

---

### Шаг 2: Создание шаблонных секций для курса (без привязки к году)

Создай секции курса (шаблоны). **Важно:** `subject_group` должен быть `null`, а `course` — ID созданного курса.  
Для шаблонов мы задаём **относительные даты**, а не конкретный год:

- `template_week_index` — номер недели учебного года (0 = первая неделя),
- `template_start_offset_days` — альтернатива: смещение в днях от `academic_start_date`,
- `template_duration_days` — длительность секции в днях (обычно 7).

```http
POST /api/course-sections/
Authorization: Bearer <superadmin_token>
Content-Type: application/json

{
  "course": 1,
  "subject_group": null,
  "title": "Неделя 1: Сложение и вычитание",
  "position": 1,
  "is_general": false,
  "template_week_index": 0,          // первая неделя учебного года
  "template_duration_days": 7        // секция длится 7 дней
}
```

**Ответ (упрощённо):**
```json
{
  "id": 10,
  "course": 1,
  "subject_group": null,
  "template_section": null,
  "title": "Неделя 1: Сложение и вычитание",
  "position": 1,
  "start_date": null,
  "end_date": null,
  "template_week_index": 0,
  "template_start_offset_days": null,
  "template_duration_days": 7,
  "is_general": false,
  "resources": [],
  "assignments": []
}
```

**Пример секции через смещение от начала года (альтернатива):**
```http
POST /api/course-sections/
Authorization: Bearer <superadmin_token>
Content-Type: application/json

{
  "course": 1,
  "subject_group": null,
  "title": "Повторение перед контрольной",
  "position": 3,
  "is_general": false,
  "template_start_offset_days": 60,  // через 60 дней после начала учебного года
  "template_duration_days": 3
}
```

---

### Шаг 3: Загрузка ресурсов в шаблонные секции

Добавь файлы, ссылки, директории в шаблонные секции:

#### 3.1. Создание файла

```http
POST /api/resources/
Authorization: Bearer <superadmin_token>
Content-Type: multipart/form-data

course_section: 10
type: file
title: Учебник глава 1.pdf
file: <binary_file_data>
```

#### 3.2. Создание директории с файлами

```http
POST /api/resources/create-directory-with-files/
Authorization: Bearer <superadmin_token>
Content-Type: multipart/form-data

course_section: 10
title: Материалы недели 1
files: <file1>, <file2>, <file3>
```

#### 3.3. Создание ссылки

```http
POST /api/resources/
Authorization: Bearer <superadmin_token>
Content-Type: application/json

{
  "course_section": 10,
  "type": "link",
  "title": "Видеоурок: Сложение",
  "url": "https://youtube.com/watch?v=...",
  "description": "Объяснение сложения для первоклассников"
}
```

#### 3.4. Создание текстового ресурса

```http
POST /api/resources/
Authorization: Bearer <superadmin_token>
Content-Type: application/json

{
  "course_section": 10,
  "type": "text",
  "title": "Важные заметки",
  "description": "Не забудьте про домашнее задание"
}
```

---

### Шаг 4: Создание заданий в шаблонных секциях (относительные даты)

Добавь задания в шаблонные секции. Для шаблонов лучше использовать **относительные** поля:

- `template_offset_days_from_section_start` — через сколько дней от `section.start_date` дедлайн,
- `template_due_time` — время суток дедлайна (например, `23:59:00`).

При синхронизации бэкенд сам посчитает `due_at` в нужном году.

```http
POST /api/assignments/
Authorization: Bearer <superadmin_token>
Content-Type: application/json

{
  "course_section": 10,                     // шаблонная секция курса
  "teacher": 5,
  "title": "Домашнее задание: Сложение до 10",
  "description": "Решите примеры на сложение",
  "template_offset_days_from_section_start": 6,   // через 6 дней от начала секции
  "template_due_time": "23:59:00",               // дедлайн в 23:59
  "max_grade": 100,
  "attachments": [
    {
      "type": "text",
      "title": "Инструкции",
      "content": "Решите все примеры на странице 5",
      "position": 1
    },
    {
      "type": "file",
      "title": "Файл с примерами",
      "file_url": "https://example.com/examples.pdf",
      "position": 2
    }
  ]
}
```

> Для существующей логики `due_at` тоже поддерживается. Если заданы шаблонные поля `template_offset_days_from_section_start` и `template_due_time`, при синхронизации они имеют приоритет и `due_at` считается автоматически.

---

### Шаг 5: Создание SubjectGroup и синхронизация контента

#### 5.1. Создай SubjectGroup для конкретного класса

```http
POST /api/subject-groups/
Authorization: Bearer <superadmin_token>
Content-Type: application/json

{
  "course": 1,
  "classroom": 3,
  "teacher": 5
}
```

**Ответ:**
```json
{
  "id": 20,
  "course": 1,
  "classroom": 3,
  "teacher": 5,
  "external_id": "..."
}
```

#### 5.2. Синхронизируй контент из курса в SubjectGroup (с учётом учебного года)

**Это ключевой эндпоинт!** Он копирует все шаблонные секции, ресурсы и задания во все SubjectGroup'ы данного курса **и автоматически рассчитывает реальные даты** на основе начала учебного года.

```http
POST /api/courses/1/sync-content/
Authorization: Bearer <superadmin_token>
Content-Type: application/json

{
  "academic_start_date": "2026-09-01"   // дата начала учебного года (опционально, но рекомендуется)
}
```

Если `academic_start_date` не передан, бэкенд сам возьмёт:
- 1 сентября текущего академического года  
  (Sep–Dec → этого года, Jan–Aug → предыдущего).

**Ответ:**
```json
{
  "detail": "Content synced to subject groups successfully."
}
```

**Что происходит:**
- Для каждой шаблонной секции курса создаётся дочерняя секция в каждом SubjectGroup.
- Для каждой шаблонной секции считаются реальные даты:
  - Находится `offset_days` из `template_start_offset_days` или `template_week_index * 7`.
  - `start_date = academic_start_date + offset_days`.
  - `end_date = start_date + (template_duration_days - 1)` (или 7 по умолчанию).
- Все ресурсы (включая вложенные директории) копируются рекурсивно.
- Все задания и их вложения копируются:
  - Если у шаблонного задания заданы `template_offset_days_from_section_start` и `template_due_time`, то:
    - `due_date = derived_section.start_date + template_offset_days_from_section_start`.
    - `due_at = due_date + template_due_time` (с таймзоной сервера).
  - Если шаблонные поля не заданы — `due_at` просто копируется.
- Каждая копия помнит связь с шаблоном через `template_section`, `template_resource`, `template_assignment`.

---

### Шаг 6: Просмотр дочерних секций

После синхронизации можно посмотреть секции конкретного SubjectGroup:

```http
GET /api/course-sections/?subject_group=20
Authorization: Bearer <token>
```

**Ответ (упрощённо):**
```json
[
  {
    "id": 30,
    "course": null,
    "subject_group": 20,
    "template_section": 10,
    "title": "Неделя 1: Сложение и вычитание",
    "position": 1,
    "start_date": "2026-09-01",
    "end_date": "2026-09-07",
    "resources": [
      {
        "id": 40,
        "template_resource": 15,
        "title": "Учебник глава 1.pdf",
        "type": "file",
        "is_unlinked_from_template": false,
        ...
      }
    ],
    "assignments": [
      {
        "id": 50,
        "template_assignment": 25,
        "title": "Домашнее задание: Сложение до 10",
        "due_at": "2026-09-07T23:59:00Z",
        "is_unlinked_from_template": false,
        ...
      }
    ]
  }
]
```

---

### Шаг 7: Добавление нового контента в шаблон (позже)

Если позже нужно добавить новые файлы/задания в родительский курс:

1. Добавь ресурсы/задания в шаблонные секции (как в шагах 3-4)
2. Снова вызови синхронизацию:

```http
POST /api/courses/1/sync-content/
Authorization: Bearer <superadmin_token>
```

**Что происходит:**
- Новые ресурсы/задания добавляются во все дочерние секции
- Старые копии не дублируются (проверка по `template_resource`/`template_assignment`)
- Если учитель отвязал ресурс/задание (`is_unlinked_from_template=true`), он не трогается

---

### Шаг 8: Отвязка ресурса/задания (для учителя)

Если учитель хочет изменить или удалить ресурс/задание только у себя (не затрагивая шаблон и другие классы):

#### 8.1. Отвязать ресурс

```http
POST /api/resources/40/unlink-from-template/
Authorization: Bearer <teacher_token>
```

**Ответ:**
```json
{
  "id": 40,
  "template_resource": 15,
  "is_unlinked_from_template": true,
  ...
}
```

**После отвязки:**
- Ресурс больше не будет обновляться при синхронизации
- Учитель может свободно редактировать/удалять его только у себя
- Другие классы не затронуты

#### 8.2. Отвязать задание

```http
POST /api/assignments/50/unlink-from-template/
Authorization: Bearer <teacher_token>
```

**Ответ:**
```json
{
  "id": 50,
  "template_assignment": 25,
  "is_unlinked_from_template": true,
  ...
}
```

---

## 🔑 Важные эндпоинты

### Курсы

| Метод | URL | Описание | Права |
|-------|-----|----------|-------|
| `POST` | `/api/courses/` | Создать курс | SuperAdmin |
| `GET` | `/api/courses/` | Список курсов | SuperAdmin |
| `GET` | `/api/courses/{id}/` | Детали курса | SuperAdmin |
| `POST` | `/api/courses/{id}/sync-content/` | **Синхронизировать контент (с учётом учебного года)** | SuperAdmin |

### Секции курса

| Метод | URL | Описание | Права |
|-------|-----|----------|-------|
| `POST` | `/api/course-sections/` | Создать секцию | RoleBased |
| `GET` | `/api/course-sections/?subject_group={id}` | Секции SubjectGroup | RoleBased |
| `GET` | `/api/course-sections/?course={id}` | Шаблонные секции курса | RoleBased |

**Важно при создании шаблонной секции:**
```json
{
  "course": 1,           // ID курса
  "subject_group": null, // ОБЯЗАТЕЛЬНО null для шаблона!
  "title": "...",
  "template_week_index": 0,              // или template_start_offset_days
  "template_duration_days": 7
}
```

**Важно при создании обычной секции:**
```json
{
  "course": null,        // null для обычной секции
  "subject_group": 20,   // ID SubjectGroup
  "title": "...",
  ...
}
```

### Ресурсы

| Метод | URL | Описание | Права |
|-------|-----|----------|-------|
| `POST` | `/api/resources/` | Создать ресурс | RoleBased |
| `GET` | `/api/resources/?course_section={id}` | Ресурсы секции | RoleBased |
| `POST` | `/api/resources/{id}/unlink-from-template/` | **Отвязать от шаблона** | RoleBased |

### Задания

| Метод | URL | Описание | Права |
|-------|-----|----------|-------|
| `POST` | `/api/assignments/` | Создать задание | RoleBased |
| `GET` | `/api/assignments/?course_section={id}` | Задания секции | RoleBased |
| `POST` | `/api/assignments/{id}/unlink-from-template/` | **Отвязать от шаблона** | RoleBased |

При создании **шаблонного задания** рекомендуется использовать поля:

```json
{
  "course_section": 10,
  "title": "...",
  "template_offset_days_from_section_start": 3,
  "template_due_time": "23:59:00",
  ...
}
```

---

## 📝 Примеры запросов

### Полный пример: создание курса с контентом

```javascript
// 1. Создать курс
const course = await fetch('/api/courses/', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    course_code: 'MATH1',
    name: 'Математика 1 класс',
    grade: 1
  })
}).then(r => r.json());

// 2. Создать шаблонную секцию
const section = await fetch('/api/course-sections/', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    course: course.id,
    subject_group: null,  // Важно!
    title: 'Неделя 1',
    position: 1
  })
}).then(r => r.json());

// 3. Добавить файл в секцию
const formData = new FormData();
formData.append('course_section', section.id);
formData.append('type', 'file');
formData.append('title', 'Учебник.pdf');
formData.append('file', fileBlob);

const resource = await fetch('/api/resources/', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`
  },
  body: formData
}).then(r => r.json());

// 4. Создать SubjectGroup
const subjectGroup = await fetch('/api/subject-groups/', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    course: course.id,
    classroom: 3,
    teacher: 5
  })
}).then(r => r.json());

// 5. Синхронизировать контент
await fetch(`/api/courses/${course.id}/sync-content/`, {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`
  }
});

// 6. Получить дочерние секции
const derivedSections = await fetch(`/api/course-sections/?subject_group=${subjectGroup.id}`, {
  headers: {
    'Authorization': `Bearer ${token}`
  }
}).then(r => r.json());
```

---

## ⚠️ Важные замечания

### 1. Различение шаблонных и дочерних секций

- **Шаблонная секция:** `course` заполнено, `subject_group = null`
- **Дочерняя секция:** `subject_group` заполнено, `course = null`, может иметь `template_section`

### 2. Синхронизация

- Синхронизация **не удаляет** существующие дочерние элементы.
- Синхронизация **добавляет только новые** элементы из шаблона.
- Если элемент уже существует (по `template_resource`/`template_assignment`), он не дублируется.
- Для секций при синхронизации:
  - `start_date`/`end_date` пересчитываются относительно `academic_start_date` и шаблонных полей.
- Для заданий при синхронизации:
  - `due_at` пересчитывается из `template_offset_days_from_section_start` + `template_due_time`, если они заданы.

### 3. Отвязка от шаблона

- После отвязки (`is_unlinked_from_template = true`) элемент больше не синхронизируется
- Учитель может свободно редактировать/удалять отвязанные элементы
- Отвязка необратима (но можно вручную вернуть `is_unlinked_from_template = false` через админку)

### 4. Права доступа

- **SuperAdmin** — может создавать курсы, шаблонные секции, синхронизировать
- **Teacher** — может работать с секциями своего SubjectGroup, отвязывать ресурсы/задания
- **Student** — только просмотр контента своего класса

### 5. Рекурсивное копирование

- Директории и их содержимое копируются рекурсивно
- Структура папок сохраняется

---

## 🎨 UI рекомендации

### Для админа (создание шаблонов):

1. **Страница "Курсы"** → кнопка "Создать курс"
2. **Страница курса** → вкладка "Шаблонные секции"
3. **В каждой секции** → загрузка файлов, создание заданий
4. **Кнопка "Синхронизировать с классами"** → вызывает `/sync-content/`

### Для учителя:

1. **Страница SubjectGroup** → показывает дочерние секции
2. **Индикатор синхронизации** → показывать, если ресурс/задание из шаблона (`template_resource`/`template_assignment` не null)
3. **Кнопка "Отвязать"** → для ресурсов/заданий, которые учитель хочет изменить только у себя

### Визуальные индикаторы:

- 🔗 Иконка "Связано с шаблоном" — если `template_resource`/`template_assignment` не null и `is_unlinked_from_template = false`
- ✂️ Иконка "Отвязано" — если `is_unlinked_from_template = true`
- 🔄 Кнопка "Синхронизировать" — только для SuperAdmin на странице курса

---

## 📞 Поддержка

Если возникнут вопросы по API, проверь:
- Swagger документацию: `/api/docs/`
- Схему API: `/api/schema/`

---

**Удачи с интеграцией! 🚀**

